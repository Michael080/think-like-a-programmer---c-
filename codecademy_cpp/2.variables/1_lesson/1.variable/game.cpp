#include <iostream>

int main() {
	int score = 0;
	// create print statement that outputs the following
	// Player score: [score]
	std::cout << "Player score: " << score << "\n";
	// << can be used to chain output in the form of strings or ints accessed via variable names
}
