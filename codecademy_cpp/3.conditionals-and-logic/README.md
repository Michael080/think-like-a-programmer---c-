# Conditionals & Logic
>[!note] tldr; 
> **`c++`** logical operators use exactly same syntax as js except:
> - **`||`** can also be written via keyword `or` (*no such thing in js methinks*)
> - **`==` equivalence** `c++` does not provide a *strict equivalence* operator `===`
